# Clients

Client-side artifacts that are not part of the Phoenix release live here.

- `nerve-extension/`: browser extension for the nerve APIs

See `../docs/clients/mcp.md` for the authenticated MCP endpoint used by tool
clients.
